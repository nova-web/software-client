(function(win) {
    //本地地址
    win.htp = {
        apihost: 'http://172.16.6.203/api/', //接口地址
        //apihost: 'http://www.v-eus.com/api/', //部署到线上环境时启用
        designWidth: 750,
        mock: true
    };
})(window);
